{
  "name": "backend",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --minify",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings",
    "migration:local": "wrangler d1 migrations apply grimore --local",
    "migration:remote": "wrangler d1 migrations apply grimore --remote"
  },
  "dependencies": {
    "@clerk/backend": "^2.16.0",
    "@hono/clerk-auth": "^3.0.3",
    "dotenv": "^17.2.2",
    "drizzle-orm": "^0.44.5",
    "hono": "^4.9.8",
    "ulid": "^3.0.1"
  },
  "devDependencies": {
    "drizzle-kit": "^0.31.5",
    "wrangler": "^4.4.0"
  }
}
